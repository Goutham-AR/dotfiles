return {
  "ibhagwan/fzf-lua",
  config = function()
    -- local fzf = require("fzf-lua")
    -- vim.keymap.set("n", "<leader>f", function()
    -- fzf.files({ cwd = vim.fn.getcwd() })
    -- end, { desc = "Find Files" })
  end,
}
